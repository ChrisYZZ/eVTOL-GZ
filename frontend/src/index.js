import React from 'react';
import ReactDOM from 'react-dom'; // 使用旧的 ReactDOM
import './index.css';
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
